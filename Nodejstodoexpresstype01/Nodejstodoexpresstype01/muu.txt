const express = require('express')
const app = express()
const port = 8080

//https://culturedhungryfirmware.zhaotan18x.repl.co/

app.set('view engine', 'ejs');

app.get('/', (req, res) => {
  res.render('index')
})

app.post('/addtask', function (req, res) {
   res.render('index')
});

app.listen(port, () => {
  console.log(`The server is running ..:${port}`)
})